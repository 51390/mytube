apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mytube.fullname" . }}-test-connection"
  labels:
    {{- include "mytube.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mytube.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
